- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.config: config
    /bin/dwm-startup: dwm-startup
    ~/.gitconfig: gitconfig
    ~/.local/share/dwm: local/share/dwm
    ~/polybar.sh: polybar.sh
    ~/.xinitrc: xinitrc
    ~/.zshrc: zshrc
    

- shell:
  - [git submodule update --init --recursive, Installing submodules]