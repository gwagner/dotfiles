#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias vi="nvim"
alias vim="nvim"
alias nano="nvim"

export EDITOR=nvim
export VISDUAL=nvim
